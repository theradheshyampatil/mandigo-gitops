apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mandigo-frontend-ingress
  namespace: default
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mandigo-frontend-service
            port:
              number: 80
